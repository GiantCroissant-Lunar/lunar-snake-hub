  # === lunar-snake-hub:start ===
- repo: local
  hooks:
  - id: hub-config
    name: Lunar Snake Hub Checks
    entry: pre-commit run --config {hubPath}/pre-commit-config.consumer.yaml
    language: system
    pass_filenames: false
    always_run: true
  # === lunar-snake-hub:end ===
