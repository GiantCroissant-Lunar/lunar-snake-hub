#!/bin/bash
# Managed by lunar-snake-hub
# Ensures pre-commit checks are run

if command -v pre-commit >/dev/null 2>&1; then
    pre-commit run "$@"
else
    echo "Warning: pre-commit not found. Skipping checks."
fi

# managed-by: lunar-snake-hub
