# GLM-4.6 API Configuration (Z.ai)
# Get your API key from: https://open.bigmodel.cn/
OPENAI_API_KEY=your-glm-jwt-token-here
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# Gateway Authentication Token
# Generate a secure random token:
# macOS/Linux: openssl rand -hex 32
# Windows PowerShell: [System.Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
GATEWAY_TOKEN=generate-random-token-here

# n8n Configuration
N8N_USER=admin
N8N_PASSWORD=changeme-please

# Optional: If using different embeddings
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSIONS=1536
